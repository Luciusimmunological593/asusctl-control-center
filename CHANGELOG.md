# Changelog

## Unreleased

- Added a thin npm wrapper in `npm-wrapper/` with system-install detection, managed-venv fallback, CLI commands, and a small JS API.
- Initial public release of the standalone open-source project.
- Added a modular PyQt6 application with `asusctl` integration and optional `supergfxctl` support.
- Added diagnostics, documentation, tests, install helpers, and desktop integration files.
